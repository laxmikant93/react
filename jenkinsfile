pipeline {
    agent { label 'slave1' }
    environment {
        SERVER = 'ubuntu@18.234.64.16'
        REMOTE_PATH = '/var/www/html'
    }
    stages {
        stage('Build') {
            steps {
                script {
                    nodejs(nodeJSInstallationName: 'nodejs') {
                        sh '''
                        cd myapp
                        npm install'''
                    }
                }
            }
        }
        stage('Test') {
            steps {
                script {
                    nodejs(nodeJSInstallationName: 'nodejs') {
                        sh '''
                        cd myapp
                        npm test'''
                    }
                }
            }
        }
        stage('Deploy') {
            steps {
                script {
                    nodejs(nodeJSInstallationName: 'nodejs') {
                        sh '''
                        cd myapp
                        npm run build'''
                    }
                    // Copy build files to Apache document root directory on slave1
                    sh '''
                    cd myapp
                    chmod 777 ./build/*
                    scp -r ./build/* ${SERVER}:${REMOTE_PATH}'''
                }
            }
        }
    }
}
